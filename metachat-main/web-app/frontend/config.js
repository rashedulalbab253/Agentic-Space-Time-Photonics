// Backend base URL used by the frontend to talk to the API and download files.
// Update this to the IP/hostname and port where your backend is reachable
// from the browser (e.g., "http://192.168.1.100:8000"). This is independent
// from the backend's own listening address.
window.BACKEND_BASE_URL = window.BACKEND_BASE_URL || "http://0.0.0.0:8000";
